# Session logs

Raw Prime Agent conversation logs (JSONL) for notable tenx development
sessions. Kept as build history: each session drove the epic/spec/ticket
loop that produced the commits around it.

- `session-2026-08-24-epc003-epc004.jsonl` — the session that shipped
  v0.8.0 → v0.11.0: packet budgets, session logging, the MCP server
  (EPC-003), review/archive, and deep SDLC linting with the 29-rule
  catalog (EPC-004).
