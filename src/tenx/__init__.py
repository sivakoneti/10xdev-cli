"""tenx — meta-harness CLI: context-as-code for AI coding agents.

The repo is no longer just code; it is a context base. tenx manages a
structured packet of markdown artifacts (epics, specs, conventions, docs),
validates the engineering process like a linter, and injects the right
context into every agent session ("benevolent prompt injection").
"""

__version__ = "0.5.0"
